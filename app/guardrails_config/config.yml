models:
  - type: main
    engine: openai
    model: nvidia/llama-3.1-nemotron-70b-instruct
    parameters:
      base_url: "https://integrate.api.nvidia.com/v1"
      api_key: $NVIDIA_NIM_API_KEY

rails:
  input:
    flows:
      - check crisis language
      - check off topic
  output:
    flows:
      - check clinical claims
